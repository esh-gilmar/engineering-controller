{
  "type": "object",
  "additionalProperties": false,
  "required": ["decision", "reason", "instructions", "risk_flags"],
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["APPROVE", "REVISE", "HUMAN_REQUIRED"]
    },
    "reason": {"type": "string"},
    "instructions": {"type": "string"},
    "risk_flags": {"type": "array", "items": {"type": "string"}}
  }
}
